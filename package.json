{
  "name": "pattern-recall",
  "private": true,
  "description": "Sync endpoint for Pattern Recall. The page itself is static and needs no build.",
  "dependencies": {
    "mongodb": "^6.10.0"
  }
}
